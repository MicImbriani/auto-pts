name: autopts_warehouse
version: "1.0.0"
config-version: 2

profile: autopts_warehouse

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  autopts_warehouse:
    silver:
      +materialized: view
    gold:
      +materialized: table
